[package]
name = "bball_3d"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18", features = ["free_camera", "bevy_gltf", "jpeg"] }
avian3d = "0.5"
bevy_brp_extras = "0.18"

# Enable a small amount of optimization in the dev profile.
[profile.dev]
opt-level = 1

# Enable a large amount of optimization in the dev profile for dependencies.
[profile.dev.package."*"]
opt-level = 3

